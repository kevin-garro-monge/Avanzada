@model IEnumerable<hibicusWeb.Tipo_Servicio>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_Tipo_Servicio }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_Tipo_Servicio }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_Tipo_Servicio })
        </td>
    </tr>
}

</table>
